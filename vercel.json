{
  "buildCommand": "cd ondragay && npm install && npm run build",
  "outputDirectory": "ondragay/dist",
  "installCommand": "cd ondragay && npm install",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}

